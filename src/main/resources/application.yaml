spring:
  datasource:
    url: jdbc:h2:mem:~
    username: user
    password: 123
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true